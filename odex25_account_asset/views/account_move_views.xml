<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_asset_inherit" model="ir.ui.view">
        <field name="name">account.move.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('oe_button_box')]" position="inside">
                <field name="asset_id" invisible="1"/>
                <field name="asset_asset_type" invisible="1"/>
                <field name="asset_ids_display_name" invisible="1"/>
                <field name="number_asset_ids" invisible="1"/>
                <field name="draft_asset_ids" invisible="1"/>
                <button name="open_asset_view"
                        attrs="{'invisible': ['|', ('asset_id', '=', False), ('asset_asset_type', '=', 'sale')]}"
                        class="oe_stat_button"
                        icon="fa-cube"
                        type="object">
                    <field name="asset_id_display_name" nolabel="1"/>
                </button>
                <button name="action_open_asset_ids"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-id-card-o"
                        attrs="{'invisible': ['|', ('number_asset_ids', '=', 0), ('draft_asset_ids', '=', True)]}">
                    <field name="asset_ids_display_name" nolabel="1"/>
                </button>
                <button name="action_open_asset_ids"
                        type="object"
                        class="oe_stat_button text-danger"
                        icon="fa-id-card-o"
                        attrs="{'invisible': ['|', ('number_asset_ids', '=', 0), ('draft_asset_ids', '=', False)]}">
                    <field name="asset_ids_display_name" nolabel="1"/>
                </button>
            </xpath>
            <xpath expr="//group[@id='header_left_group']" position="inside">
                <group>
                    <field name="authorized_person_id" attrs="{'invisible':[('move_type','=','entry')]}"/>
                    <field name="authorized_signature" widget="signature"
                           attrs="{'invisible':[('move_type','=','entry')]}"/>
                    <field name="journal_type_id" attrs="{'invisible':[('move_type','!=','entry')]}"/>
                </group>
            </xpath>
            <xpath expr="//field[@name='line_ids']/tree/field[@name='partner_id']" position="replace">
                <field name="journal_type"/>
                <field name="partner_id" domain="['|', ('parent_id', '=', False), ('is_company', '=', True)]"
                       attrs="{'column_invisible': [('parent.move_type', '!=', 'entry')] ,'required':[('journal_type','=','صرف عهدة')]}"/>

            </xpath>
        </field>
    </record>

    <record id="view_move_line_form_asset_inherit" model="ir.ui.view">
        <field name="name">account.move.line.form</field>
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_move_line_form"/>
        <field name="arch" type="xml">
            <field name="move_id" position="after">
                <field name="asset_ids" attrs="{'invisible': [('asset_ids','=',[])]}"/>
            </field>
        </field>
    </record>

    <record model="ir.actions.server" id="action_account_aml_to_asset">
        <field name="name">Create Asset</field>
        <field name="groups_id" eval="[(4, ref('account.group_account_user'))]"/>
        <field name="model_id" ref="account.model_account_move_line"/>
        <field name="binding_model_id" ref="account.model_account_move_line"/>
        <field name="state">code</field>
        <field name="code">
            if records:
            action = records.turn_as_asset()
        </field>
    </record>

    <record id="view_res_partner_form_asset_inherit" model="ir.ui.view">
        <field name="name">res.partner.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <field name="vat" position="after">
                <field name="authorized_person_id"/>
                <field name="signature" widget="signature"/>
            </field>
            <xpath expr="//field[@name='category_id']" position="before">
                <field name="company_id" widget="many2one_tag" readonly="0"/>
            </xpath>
        </field>
    </record>
    <data noupdate="1">
        <record id="journal_type_sarfo_ohda" model="account.journal.type">
            <field name="name">صرف عهدة</field>
        </record>
    </data>
</odoo>
